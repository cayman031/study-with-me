spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/studywithme_test?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    username: ${DB_USERNAME:studywithme}
    password: ${DB_PASSWORD:studywithme}

  jpa:
    properties:
      hibernate:
        show_sql: false

jwt:
  secret: ${JWT_SECRET:test-secret-32bytes-length!!!!--}
  access-ttl-minutes: ${JWT_ACCESS_TTL_MINUTES:15}
  refresh-ttl-days: ${JWT_REFRESH_TTL_DAYS:7}
